/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        // This creates the 'font-grotesk' class
        grotesk: ['"Power Grotesk"', 'sans-serif'], 
      },
    },
  },
  plugins: [],
}